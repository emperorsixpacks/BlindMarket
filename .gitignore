node_modules/
dist/
.env
.env.local
.env.production
*.log
.DS_Store
coverage/
cache/
artifacts/
typechain-types/

# Local storage fallback (dev only)
.storage/

# Contract build artifacts (not the source)
contracts/cache/
contracts/artifacts/

# Ignore secrets but keep deployment addresses
contracts/.env
.aidesigner/*
!.aidesigner/.gitkeep

# SQLite data directory
backend/data/

# Local tool configs
.agents/
.claude/
.mcp.json

# Git worktrees
.worktrees/
kiro.json

# Design system zip (use design_handoff_blindbounty/ instead)
BlindBounty Design System.zip

# TypeScript incremental build cache
*.tsbuildinfo
.kiro.json
dump.rdb
scratch/

backend/.env.testnet
frontend/.env.testnet

# Celo multi-chain — local-only WIP, not for production
docs/CELO-MULTI-CHAIN.md
